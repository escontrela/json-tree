# bootstrap

Contiene el arranque de la app, la inicialización de Spring Boot, el registro del `Stage` principal y la infraestructura necesaria para cargar FXML con controladores gestionados por Spring.
